apps:
  - script: ./dist/main.js
    name: paymnts-app
    watch: true
    instances: max
    exec_mode: cluster
    env:
      PORT: ${PORT}
      NODE_ENV: development
    env_production:
      PORT: ${PORT}
      NODE_ENV: production
